# aws-for-dev-ec2-cloudformation

* Create stack:
`aws cloudformation create-stack --stack-name EC2-INSTANCE --template-body file://ec2.yaml`
* Delete stack:
`aws cloudformation delete-stack --stack-name EC2-INSTANCE`